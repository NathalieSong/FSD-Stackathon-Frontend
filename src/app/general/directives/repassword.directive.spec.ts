import { RepasswordDirective } from './repassword.directive';

describe('RepasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new RepasswordDirective();
    expect(directive).toBeTruthy();
  });
});
